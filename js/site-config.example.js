/**
 * Copy to site-config.js and add your Google Maps API key.
 * Enable "Maps JavaScript API" + "Places API" in Google Cloud Console.
 * Restrict the key by HTTP referrer (e.g. localhost:8000, your domain).
 */
window.NostalgiaSiteConfig = {
  googleMapsApiKey: "",
};
